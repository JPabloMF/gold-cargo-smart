{
  "installCommand": "cd spa && pnpm install --ignore-workspace",
  "buildCommand": "cd spa && pnpm run build",
  "outputDirectory": "spa/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
